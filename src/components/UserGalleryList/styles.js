import {moderateScale} from '@/theme/scaling';
import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  userGalleyContainer: {
    paddingHorizontal: moderateScale(26, 0.3),
  },
});
